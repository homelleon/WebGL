import {OBJLoader} from './../../../../source/engine/loader/OBJLoader';

function testOBJLoader() {
	describe('.obj file loader', () => {
		var loader = new OBJLoader();
		it('should return same mesh', () => {
			
		});
	});
}